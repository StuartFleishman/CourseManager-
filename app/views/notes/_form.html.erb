<% if @note.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@note.errors.count, "error") %> prohibited this course from being saved:</h2>

    <ul>
      <% @note.errors.each do |error| %>
        <li><%= error.full_message %></li>
      <% end %>
    </ul>
  </div>
<% end %>






<%= form_for([@course, @note]) do |f| %>
  <%= display_course_fields(@course, f) %>
<br>
<%= f.label 'content' %>
  <%= f.text_area :content%>
  <br>
  <%= f.submit %>
<%end%>