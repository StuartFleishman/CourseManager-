Course Name: <%= @note.course.name %>
<br>
Content:
<%= @note.content %>

<br>
<%= link_to 'Edit', edit_note_path(@note) %>

<br>
<%= link_to 'Delete Note', note_path(@note.id),
               :confirm => 'Are you sure?',
               :method => :delete %>