<h1>Notes</h1>

<%= display_header(@course) %>

<ol>
<% @notes.each do |note| %>
  <li> <%= link_to(note.content, note_path(note)) %> </li>
<%end %>
</ol>

 <h3>last created note </h3>
 <%= @notes.last_created_note %>

